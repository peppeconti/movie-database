import classes from "./Footer.module.css";

const Footer = () => {
  return <footer className={classes.footer}>
    <p>Made by Giuseppe Conti using React and the OMDb API</p>
  </footer>;
};

export default Footer;